# Pre-made Library Sequencing 기본 정보

## 1) Sample Requirement (manual)
- 해당 서비스 블록에서 명확한 `## Sample Requirement(s)` 섹션을 찾지 못함 (수동 확인 필요).

## 2) BI content / Analysis (manual)
- 해당 서비스 블록에서 명확한 BI/Analysis 섹션을 찾지 못함 (수동 확인 필요).

## 3) 권장 output (Gb or M reads)
- | Clean data release | Per 100 Gb / (Minimum 100 Gb) | 30000 | 27000 | 1. If client applys clean data release; / 2. Double data size for data release |
- | Novaseq X plus - PE150 | D < 50Gb (Per project) | 13500 | 12150 | 1. If pooling by Novogene, when customer’s library pass our library QC, we guarantee the data output for each 'Pass' library. / 2. If pooling by customer, when customer’s library pass our library QC, we guarantee the data output for the pooled library. / 3. Minimum 1Gb data per library; / 4. Each library contains at most 10 sub libraries/index number, or else need to order more data based on sub library numbers/index numbers (every 10 indexs order minimum 1Gb) ; / 5. Free of charge for up to 10 index for each Gb. If 1Gb contains more than 10 index, we will charge demultiplexing fee based on index numbers. Fees = (index number-10)* Unit price. |
- | DNBSEQ-T7 PE150 | D < 50Gb (Per project) | 10500 | 9000 | 1. If pooling by Novogene, when customer’s library pass our library QC, we guarantee the data output for each 'Pass' library. / 2. If pooling by customer, when customer’s library pass our library QC, we guarantee the data output for the pooled library. / 3. Minimum 1Gb data per library; / 4. Each library contains at most 10 sub libraries/index number, or else need to order more data based on sub library numbers/index numbers (every 10 indexs order minimum 1Gb) ; / 5. Free of charge for up to 10 index for each Gb. If 1Gb contains more than 10 index, we will charge demultiplexing fee based on index numbers. Fees = (index number-10)* Unit price. |
- | NovaSeq 6000 PE150 | D < 50Gb (Per project) | 19500 | 18000 | 1. Minimum 1Gb data per library; / 2. Each library contains at most 10 sub libraries/index number, or else need to order more data based on sub library numbers/index numbers (every 10 indexs order minimum 1Gb) ; / 3. Free of charge for up to 10 index for each Gb. If 1Gb contains more than 10 index, we will charge demultiplexing fee based on index numbers. Fees = (index number-10)* Unit price. |
- | Data trim QC | Per 100 Gb / (Minimum 100 Gb) | 30000 | 27000 | Minimum 20 USD per case |
- | Clean data release | Per 100 Gb / (Minimum 100 Gb) | 30000 | 27000 | 1. If client applys clean data release; / 2. Double data size for data release |

## 4) 금액 (Sales Price, WON)
| Library QC/ library ( for Illumina/ MGI platform) |  | 24000 | 19500 |  |
| Novaseq X plus-10B- PE150 | Lane sequencing: / 375Gb raw data per lane* | 2700000 | 2430000 | 1. 10B PML guarantees a data output of 375 Gb per lane, subject to the following conditions: / 1) Base-balanced library, with each lane having ≥5 indexes, and all library QC in the PML graded as pass. / 2) The theoretical data output of >375 Gb includes index, Phix data, demultiplexed data, and data that cannot be multiplexed. / / 2. On average per order, free of charge for up to 100 index for each lane. If more than 100 index per lane, we will charge demultiplexing fee based on index numbers. Fees = (index number-100)* Unit price. / 3. On average per order, not accept demultiplexing if index number per lane is above 384. |
| Novaseq X plus-10B- PE150 | Flowcell sequencing: / 3000Gb raw data per FC* | 21000000 | 18900000 | 1. 10B PML guarantees a data output of 3000 Gb per FC, subject to the following conditions: / 1) Base-balanced library, with each lane having ≥5 indexes, and all library QC in the PML graded as pass. / 2) The theoretical data output of >3000 Gb includes index, Phix data, demultiplexed data, and data that cannot be multiplexed. / / 2. On average per order, free of charge for up to 100 index for each lane. If more than 100 index per lane, we will charge demultiplexing fee based on index numbers. Fees = (index number-100)* Unit price. / 3. On average per order, not accept demultiplexing if index number per lane is above 384. / / 3. 1 Flowcell=8 lanes |
| Novaseq X plus-25B- PE150 | Lane sequencing: / 1Tb raw data per lane* | 6000000 | 5400000 | 1. 25B PML guarantees a data output of 1Tb per lane, subject to the following conditions: / 1) Base-balanced library, with each lane having ≥5 indexes, and all library QC in the PML graded as pass. / 2) The theoretical data output of >1Tb includes index, Phix data, demultiplexed data, and data that cannot be multiplexed. / / 2. On average per order, free of charge for up to 100 index for each lane. If more than 100 index per lane, we will charge demultiplexing fee based on index numbers. Fees = (index number-100)* Unit price. / 3. On average per order, not accept demultiplexing if index number per lane is above 384. |
| Novaseq X plus-25B- PE150 | Flowcell sequencing: / 8Tb raw data per FC* | 48000000 | 42000000 | 1. 25B PML guarantees a data output of 8Tb per FC, subject to the following conditions: / 1) Base-balanced library, with each lane having ≥5 indexes, and all library QC in the PML graded as pass. / 2) The theoretical data output of >8Tb includes index, Phix data, demultiplexed data, and data that cannot be multiplexed. / / 2. On average per order, free of charge for up to 100 index for each lane. If more than 100 index per lane, we will charge demultiplexing fee based on index numbers. Fees = (index number-100)* Unit price. / 3. On average per order, not accept demultiplexing if index number per lane is above 384. / / 3. 1 Flowcell=8 lanes |
| NovaSeq 6000- S4- PE150 | Lane sequencing: / 800Gb raw data per lane | 8250000 | 7950000 | 1. On average per order, free of charge for up to 100 index for each lane. If more than 100 index per lane, we will charge demultiplexing fee based on index numbers. Fees = (index number-100)* Unit price. / 2 On average per order, not accept demultiplexing if index number per lane is above 384. |
| NovaSeq 6000- S4- PE150 | Flowcell sequencing: / 3200Gb raw data per FC | 32700000 | 30000000 | 1 Flowcell=4 lanes |
| DNBSEQ-T7 PE150 | FC sequencing: / (only promise 1700Gb raw data for base balanced library and library QC result is Pass level) | 7050000 | 6345000 | 1. On average per order, free of charge for up to 100 index for each lane. If more than 100 index per lane, we will charge demultiplexing fee based on index numbers. Fees = (index number-100)* Unit price. / 2 On average per order, not accept demultiplexing if index number per lane is above 384. |
| DNBSEQ-T7 150 cycle | Stereo seq libraries for both OCT and FFPE | 7050000 | 6750000 | 1. 1 Flowcell=1 lane. / 2. Premade libraries from OCT-embedded samples and FFPE samples can NOT sequence in the same lane. |
| DNBSEQ-T7 PE100 |  | 7050000 | 6750000 | 1. 1 Flowcell=1 lane. / 2. Available for MGI C4 single cell library. |
| NovaSeq PE250 | 400M pair reads per lane | 7050000 | 6345000 |  |
| Novaseq SE50 | 800M pair reads per Flowcell | 6900000 | 6210000 | Only accept Flowcell sequencing. |

---
- Manual source block: `Novogene Product Manual – Pre-made Library Sequencing`
- Price source section keyword: `PMP, PML`