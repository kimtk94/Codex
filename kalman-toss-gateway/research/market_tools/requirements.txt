# Isolated research environment. Never install into the Toss production or Market Data V2 venvs.
vectorbt==1.1.0
# vectorbt 1.1.0 still references scattermapbox templates removed in Plotly 7.
plotly<7
pyarrow>=17
python-dotenv>=1.0
